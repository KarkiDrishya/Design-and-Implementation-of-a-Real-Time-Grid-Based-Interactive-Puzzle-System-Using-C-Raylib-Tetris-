//this file is made to hold the rotation states in key value pairs using "map" function in c++

#pragma once
class Position{
    public:
    Position(int row, int column);
    int row;
    int column;
};