//basically this file is the file which contains the outer blocks of the main inside blocks(more in the presentaion)

#pragma once
class Block{ //Block made for inheritance of other 8 big blocks
    public:
    Block();
    int id;
    private:
};